# Pluto Metrics System

Pluto keeps log of the metrics. It attaches directly to the `/sys/` hierarchy and writes them to the core database. 

A TUI frontend is used to query the metrics with plain SQL queries.
